---
title: "多項式を非斉次項にもつ場合"
---

次の各小問において, 微分方程式
$$
P(D)y=f(x)
$$
の非斉次項 $f(x)$ の形に応じて, 特殊解を一つ求めよ. ただし, $D=\tfrac{d}{dx}$ とし, $P(\lambda)$ を多項式とする.

## (1) $f(x)\equiv1$, $0$ が非特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="constant-nonzero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generateConstantNonzero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="constant-nonzero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y = \frac{1}{P(0)}
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

まず, 定数解 $y=y_0$ の形で探せば良いことは認める (理由は補足参照).

定数であるため, 
$$
Dy_0=0
$$ 
であり, このことから微分作用素 $P(D)$ を $y=y_0$ に作用させたときに, $P(D)$ の定数項以外の部分はすべて消えることがわかる. $P(D)$ の定数項は当然 $P(0)$ であるため,
$$
P(D)y_0=P(0)y_0.
$$
よって, 与式 
$$
P(D)y=1
$$ 
に代入して, 
$$
P(0)y_0=1, 
\quad \text{\emph{i.e.}}, \quad 
y_0=\frac{1}{P(0)}
$$ 
を得る.

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

定数解だけ考えれば良い理由は次の通り：

まず, 混乱を避けるため, 求める特殊解を $y_p$ としておく. 与式の両辺を微分して, 
$$
DP(D)y_p=0
$$ 
を得る. $\lambda P(\lambda)=0$ の特性根 $0$ が多重度 $1$ であることから, $y_p$ は 
$$ 
Dy_1=0, \quad P(D)y_2=0
$$ 
それぞれの一般解の和
$$
y_p=y_1+y_2
$$
で表せる. ここで, $y_2$ は与式の斉次解でもあることから,
特殊解 $y_p$ を探すときに無視してよいことがわかり, $y_p$ として
$$
Dy_1=0
$$ 
の一般解の形, すなわち, 定数の形で探せば良いことがわかる.

:::

::::

:::::

## (2) $f(x)\equiv1$, $0$ が特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="constant-zero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generateConstantZero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="constant-zero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y = \frac{x^m}{m!\,Q(0)}
$$
ただし, $m$ を $P(\lambda)=0$ の特性根 $0$ の多重度とし, 多項式 $Q(\lambda)$ を $P(\lambda)/\lambda^m$ で定める. すなわち, 整数 $m\geq1$ と多項式 $Q(\lambda)$ を次が成り立つように定める： 
$$
P(\lambda) = \lambda^m Q(\lambda), \quad Q(0) \neq 0.
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

\(1) に帰着させるため, まず
$$
u=D^my
$$ 
とおく. このとき,
$$
Q(D)u=1
$$ 
であるから, (1) より, 特殊解
$$
u = \frac{1}{Q(0)}
$$
を得る. これに $u=D^my$ を代入して, 
$$
D^my = \frac{1}{Q(0)}
$$ 
を得るので, 求める特殊解 $y$ はこの右辺を $m$ 回積分すれば得られる.
ここで, 不定積分では積分定数に由来する斉次解が加わるだけなので, 毎回の積分定数を $0$ として計算すれば良い.
よって,
$$
\begin{aligned}
y &= \int_0^x \cdots \int_0^{x_2} \frac{1}{Q(0)} \, dx_1\, \cdots \,dx_m \\
&= \frac{1}{m!} \frac{x^m}{Q(0)}
\end{aligned}
$$

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

\(1) との本質的な違いは $y$ が $D^my$ に変化した点のみ.  
実はこれを意識して, 問題の式もあえて $D^my$ という塊ができるように出題した.  
果たしてこれに気づいてくれただろうか$\ldots$

:::

::::

:::::

## (3) $f(x)=x^\ell$, $0$ が非特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="monomial-nonzero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generateMonomialNonzero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="monomial-nonzero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y = \sum_{k=0}^{\ell} c_k \frac{\ell!}{(\ell-k)!} x^{\ell-k}
$$
ただし, $c_k$ は $\frac{1}{P(z)}$ をマクローリン展開した時の係数として次のように定める：
$$
\frac{1}{P(z)}=c_0+c_1z+c_2z^2+\cdots.
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

特殊解を見つければ良いだけなので, 数学的な厳密性は脇に置いておいて,
$$
y=\frac{1}{P(D)}x^\ell
$$ 
として計算することを考える.
まず, $P(0)\neq0$ なので, $\frac{1}{P(z)}$ は $z=0$ のまわりでテイラー展開できる.
このテイラー展開の式において, $z$ を $D$ に置き換えることで, 
$$
\frac{1}{P(D)}=c_0+c_1D+c_2D^2+\cdots
$$ 
を得る.
ここで,
$$
D^{\ell+1}x^\ell=0
$$ 
であることに注意すると, $k=\ell$ まで計算すれば良いことがわり, よって,
$$
\begin{aligned}
\frac{1}{P(D)} x^\ell 
&= \left( c_0 + c_1 D + \cdots + c_\ell D^\ell \right) \, x^\ell \\
&= \sum_{k=0}^{\ell} c_k \frac{\ell!}{(\ell-k)!} x^{\ell-k}
\end{aligned}
$$
を得る.

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

\(1) を参考に 
$$
D^\ell P(D)y=\ell!
$$ 
から考えてくれた学生もいると思う. 
こうすると一見 \(2) に帰着するわけだが, ここではまだ見つけるべき $y$ の形がわかっていないので, \(2) の *特殊解* を採用するわけにはいかない. 

では更にもう一回微分した
$$
D^{\ell+1}P(D)y=0
$$ 
を考えた場合はどうであろうか？ 
こちらを考えてくれた学生もいると思うが, 実はこちらの方が (1) の考え方に忠実で素直なため, 先ほどより少し見通しが良い. 
実際, 見つけるべき特殊解 $y_p$ は 
$$
D^{\ell+1}y_1=0, \quad P(D)y_2=0
$$ 
それぞれの一般解の和
$$
y_p=y_1+y_2
$$
と表せる. よって, \(1) のときと同様, $y_2$ が与式の斉次解より, $y_p$ を探すときに無視してよく, $y_1$ の形で探せば良いことがわかる. こうして次の別手法を得るわけである.

特殊解として
$$
y=a_0 + a_1 x + \cdots + a_\ell x^\ell
$$ 
の形を考えて与式に代入し, 恒等式の係数比較をする. 
それにより $\ell+1$ 本の式からなる連立方程式を得るので, それを解いて $a_0,\dots,a_\ell$ の値を決定する. 
ただし, この方法は意外と計算が面倒なため, 上の計算手法が実は楽なのである.

:::

::::

:::::

## (4) $f(x)=x^\ell$, $0$ が特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="monomial-zero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generateMonomialZero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="monomial-zero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y
=
\sum_{j=0}^{\ell}
c_j
\frac{\ell!}{(\ell-j+m)!} x^{\ell-j+m}
$$
ただし, 
$P(D)=Q(D)D^m$, $Q(0)\neq0$ とし,
$c_k$ は $\frac{1}{Q(z)}$ をマクローリン展開した時の係数として次のように定める：
$$
\frac{1}{Q(z)}=c_0+c_1z+c_2z^2+\cdots.
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

- $u=D^my$ とおいて, $Q(D)u=x^\ell$ を得る.
- $Q(D)u=x^\ell$ の特殊解は
  $$
  u
  =
  \sum_{j=0}^{\ell}
  c_j
  \frac{\ell!}{(\ell-j)!}
  x^{\ell-j}.
  $$
- $D^my=u$ を積分定数 $0$ として $m$ 回積分し, 特殊解を得る.

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

- これまでの問題から, 今回の解法に自力で気づいた方が多かったと思われる. 
- \(3) との本質的な違いは, \(1) と \(2) のときの関係と同様で, $y$ が $D^my$ に置き換わっていることだけである.

:::

::::

:::::

## (5) 多項式, $0$ が非特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="polynomial-nonzero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generatePolynomialNonzero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="polynomial-nonzero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y
=
\sum_{i=0}^{\ell}
\left(
\sum_{j=i}^{\ell}
b_j c_{j-i}\frac{j!}{i!}
\right)
x^i
$$
ただし, 
$$
f(x)=b_0 + b_1 x + \cdots + b_\ell x^\ell
$$
とし, $c_k$ は $\frac{1}{P(z)}$ をマクローリン展開した時の係数として次のように定める：
$$
\frac{1}{P(z)}=c_0+c_1z+c_2z^2+\cdots.
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

- \(3) より, 各単項式 $x^j$ に対して,
  $$
  \frac{1}{P(D)}x^j
  =
  \sum_{k=0}^{j}
  c_k
  \frac{j!}{(j-k)!}
  x^{j-k}.
  $$
- 作用素 $\tfrac{1}{P(D)}$ は線形なので,
  これを $b_j$ 倍して, 足し合わせれば,
  $$
  \frac{1}{P(D)}f(x)
  =
  \sum_{j=0}^{\ell}
  b_j
  \sum_{k=0}^{j}
  c_k
  \frac{j!}{(j-k)!}
  x^{j-k}.
  $$
- $i=j-k$ とおいて, 和の順序を交換すれば,
  $$
  \begin{aligned}
    \frac{1}{P(D)}f(x)
    &=
    \sum_{j=0}^{\ell}
    b_j
    \sum_{i=0}^{j}
    c_{j-i}
    \frac{j!}{i!}
    x^i \\
    &=
    \sum_{i=0}^{\ell}
    \left(
    \sum_{j=i}^{\ell}
    b_j c_{j-i}\frac{j!}{i!}
    \right)
    x^i.
  \end{aligned}
  $$

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

- \(5) は \(3) を足し合わせただけで, 計算自体に新しいことはない.
- 公式だけを見ると少し重たく見えるが, やっていることは各単項式に対する計算の足し算で, 公式を使って計算する人はまずいないだろう.
- 公式の導出自体はそれなりに面白く, 最後に $x^i$ の係数として整理するため, $i=j-k$ と置き換えている.
- 和の交換をする際, $i,j$ の動かし方が変わることに気をつけよう. 足し算は  $0\leq i\leq j\leq\ell$ の範囲で行われるので, $j$ を止めて $i$ で先に足し上げるときは $0\leq i\leq j$, $i$ を止めて $j$ で先に足し上げるときは $i\leq j\leq\ell$ であることに注意する.

:::

::::

:::::

## (6) 多項式, $0$ が特性根

::::: {.callout-note title="問題" appearance="default" icon=false}

<div id="polynomial-zero-problem"></div>
<div class="problem-button-row">
  <button type="button" class="problem-button" onclick="generatePolynomialZero()">問題を変更</button>
</div>

:::: {.callout-note title="解答と解説" appearance="default" icon=false collapse=true}

::: {.callout-important title="解答" appearance="default" icon=false}

<div id="polynomial-zero-answer"></div>

:::

::: {.callout-tip title="公式" appearance="default" icon=false}

$$
y
=
\sum_{i=0}^{\ell}
\left(
\sum_{j=i}^{\ell}
b_j c_{j-i}\frac{j!}{(i+m)!}
\right)
x^{i+m}
$$
ただし, 
$$
f(x)=b_0 + b_1 x + \cdots + b_\ell x^\ell
$$
とし, $P(D)=Q(D)D^m$, $Q(0)\neq0$ とする. また, $c_k$ は $\frac{1}{Q(z)}$ をマクローリン展開した時の係数として次のように定める：
$$
\frac{1}{Q(z)}=c_0+c_1z+c_2z^2+\cdots.
$$

:::

::: {.callout-tip title="公式の導出" appearance="default" icon=false}

- $u=D^my$ とおいて, $Q(D)u=f(x)$ を得る.
- \(5) より, $Q(D)u=f(x)$ の特殊解は
  $$
  u
  =
  \sum_{i=0}^{\ell}
  \left(
  \sum_{j=i}^{\ell}
  b_j c_{j-i}\frac{j!}{i!}
  \right)
  x^i.
  $$
- $D^my=u$ を積分定数 $0$ として $m$ 回積分すれば, 特殊解 $y$ が得られる.
- 実際,
  $$
  x^i
  $$
  を $m$ 回積分すると
  $$
  \frac{i!}{(i+m)!}x^{i+m}
  $$
  となるので,
  $$
  \begin{aligned}
  y
  &=
  \sum_{i=0}^{\ell}
  \left(
  \sum_{j=i}^{\ell}
  b_j c_{j-i}\frac{j!}{i!}
  \right)
  \frac{i!}{(i+m)!}x^{i+m} \\
  &=
  \sum_{i=0}^{\ell}
  \left(
  \sum_{j=i}^{\ell}
  b_j c_{j-i}\frac{j!}{(i+m)!}
  \right)
  x^{i+m}.
  \end{aligned}
  $$

:::

::: {.callout-tip title="補足" appearance="default" icon=false}

- ここまで丁寧に見ていただいた学生はすでに気づいていると思うが, このページの問題において, 公式を覚える必要は全くない.
- いずれも $y=\tfrac{1}{D^m}\tfrac{1}{Q(D)}f(x)$ を計算するだけである. あとは, $\tfrac{1}{Q(D)}$ のテイラー展開を利用するだけである.
- もちろん $y=\tfrac{1}{Q(D)}\tfrac{1}{D^m}f(x)$ などでも良いが, 最後に積分をした方が計算量は少なく済むのも, 細かい注意点だろう.
- $\tfrac{1}{Q(D)}$ は本来, 積分核を用いて表される積分作用素である. しかし, 多項式 $f(x)$ に作用させる場合, $\tfrac{1}{Q(D)}$ を有限回の微分操作に置き換えられる. 
- この性質が非常に強く, このページの問題を簡単にしている一番の要因となっている. 
<!-- 補足最後の説明では、次のページで指数型を作ることを考えて、それに繋がるような締めにすることを意識しました。作業を再開するときはこの意識を思い出してもらえると嬉しいです。 -->

:::

::::

:::::

<script>
function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

function range(min, max) {
  const out = [];
  for (let i = min; i <= max; i++) out.push(i);
  return out;
}

function nonzeroRand(min, max) {
  let n = 0;
  while (n === 0) n = randInt(min, max);
  return n;
}

function gcd(a, b) {
  a = Math.abs(a);
  b = Math.abs(b);
  while (b !== 0) {
    const t = a % b;
    a = b;
    b = t;
  }
  return a || 1;
}

class Fr {
  constructor(num, den = 1) {
    if (den === 0) throw new Error("zero denominator");
    if (den < 0) {
      num = -num;
      den = -den;
    }
    const g = gcd(num, den);
    this.n = num / g;
    this.d = den / g;
  }
  add(other) { return new Fr(this.n * other.d + other.n * this.d, this.d * other.d); }
  sub(other) { return new Fr(this.n * other.d - other.n * this.d, this.d * other.d); }
  mul(other) { return new Fr(this.n * other.n, this.d * other.d); }
  div(other) { return new Fr(this.n * other.d, this.d * other.n); }
  neg() { return new Fr(-this.n, this.d); }
  isZero() { return this.n === 0; }
  abs() { return new Fr(Math.abs(this.n), this.d); }
}

function fact(n) {
  let out = 1;
  for (let i = 2; i <= n; i++) out *= i;
  return out;
}

function fracToTex(fr) {
  if (fr.d === 1) return String(fr.n);
  if (fr.n < 0) return `-\\frac{${-fr.n}}{${fr.d}}`;
  return `\\frac{${fr.n}}{${fr.d}}`;
}

function fracAbsToTex(fr) {
  const a = fr.abs();
  if (a.d === 1) return String(a.n);
  return `\\frac{${a.n}}{${a.d}}`;
}

function monomialTex(power) {
  if (power === 0) return "1";
  if (power === 1) return "x";
  return `x^${power}`;
}

function dPowerTex(power) {
  if (power === 1) return "D";
  return `D^${power}`;
}

function dInverseTex(power) {
  if (power === 1) return "D^{-1}";
  return `D^{-${power}}`;
}

function coeffPowerTerm(coeff, variable, power) {
  if (coeff === 0) return "";
  const absCoeff = Math.abs(coeff);
  let body = "";
  if (power === 0) {
    body = String(absCoeff);
  } else {
    const v = power === 1 ? variable : `${variable}^${power}`;
    body = absCoeff === 1 ? v : `${absCoeff}${v}`;
  }
  return coeff < 0 ? `-${body}` : body;
}

function signedJoin(terms) {
  const nonempty = terms.filter(t => t !== "");
  if (nonempty.length === 0) return "0";
  let out = nonempty[0];
  for (let i = 1; i < nonempty.length; i++) {
    if (nonempty[i].startsWith("-")) out += nonempty[i];
    else out += "+" + nonempty[i];
  }
  return out;
}

function operatorTex(coeffs) {
  const terms = coeffs.map((c, i) => coeffPowerTerm(c, "D", i));
  return signedJoin(terms);
}

function lambdaPolynomialTex(coeffs) {
  const terms = coeffs.map((c, i) => coeffPowerTerm(c, "\\lambda", i));
  return signedJoin(terms);
}

function polynomialTex(coeffs) {
  const terms = coeffs.map((c, i) => coeffPowerTerm(c, "x", i));
  return signedJoin(terms);
}

function polynomialFromFractionsTex(coeffs) {
  const terms = [];
  for (let i = coeffs.length - 1; i >= 0; i--) {
    const c = coeffs[i];
    if (c.isZero()) continue;
    const abs = fracAbsToTex(c);
    let body = "";
    if (i === 0) {
      body = abs;
    } else {
      const xpart = i === 1 ? "x" : `x^${i}`;
      body = abs === "1" ? xpart : `${abs}${xpart}`;
    }
    terms.push((c.n < 0 ? "-" : "") + body);
  }
  return signedJoin(terms);
}

function randomOperatorCoeffs(deg, nonzeroConstant = true) {
  const coeffs = [];
  for (let i = 0; i <= deg; i++) {
    if (i === 0 && nonzeroConstant) coeffs.push(nonzeroRand(-4, 4));
    else if (i === deg) coeffs.push(nonzeroRand(-3, 3));
    else coeffs.push(randInt(-3, 3));
  }
  return coeffs;
}

function randomPolynomialCoeffs(deg) {
  const coeffs = [];
  for (let i = 0; i <= deg; i++) {
    if (i === deg) coeffs.push(nonzeroRand(-3, 3));
    else coeffs.push(randInt(-3, 3));
  }
  return coeffs;
}

function inverseSeries(coeffs, maxDegree) {
  const c = coeffs.map(v => new Fr(v));
  const b = [new Fr(1).div(c[0])];
  for (let n = 1; n <= maxDegree; n++) {
    let sum = new Fr(0);
    for (let k = 1; k <= Math.min(n, coeffs.length - 1); k++) {
      sum = sum.add(c[k].mul(b[n - k]));
    }
    b[n] = sum.neg().div(c[0]);
  }
  return b;
}

function derivativePolynomial(poly, k) {
  let current = poly.map(v => v instanceof Fr ? v : new Fr(v));
  for (let t = 0; t < k; t++) {
    const next = [];
    for (let i = 1; i < current.length; i++) {
      next.push(current[i].mul(new Fr(i)));
    }
    current = next.length === 0 ? [new Fr(0)] : next;
  }
  return current;
}

function addPoly(a, b) {
  const n = Math.max(a.length, b.length);
  const out = [];
  for (let i = 0; i < n; i++) {
    out[i] = (a[i] || new Fr(0)).add(b[i] || new Fr(0));
  }
  return trimPoly(out);
}

function scalePoly(a, scalar) {
  return trimPoly(a.map(v => v.mul(scalar)));
}

function trimPoly(poly) {
  const out = poly.slice();
  while (out.length > 1 && out[out.length - 1].isZero()) out.pop();
  return out;
}

function applyInverseOperator(coeffs, poly) {
  const degree = poly.length - 1;
  const inv = inverseSeries(coeffs, degree);
  let out = [new Fr(0)];
  for (let k = 0; k <= degree; k++) {
    const deriv = derivativePolynomial(poly, k);
    out = addPoly(out, scalePoly(deriv, inv[k]));
  }
  return trimPoly(out);
}

function integratePoly(poly, r) {
  let out = poly.map(v => v instanceof Fr ? v : new Fr(v));
  for (let t = 0; t < r; t++) {
    const next = [new Fr(0)];
    for (let i = 0; i < out.length; i++) {
      next[i + 1] = out[i].div(new Fr(i + 1));
    }
    out = next;
  }
  return trimPoly(out);
}

function setMath(id, tex) {
  const element = document.getElementById(id);
  element.innerHTML = "";

  if (window.MathJax && window.MathJax.typesetPromise) {
    element.innerHTML = `\\[${tex}\\]`;
    window.MathJax.typesetPromise([element]);
  } else if (window.katex) {
    window.katex.render(tex, element, {
      displayMode: true,
      throwOnError: false
    });
  } else {
    element.textContent = tex;
  }
}

function answerTex(lines) {
  return `\\begin{aligned}${lines.join("\\\\")}\\end{aligned}`;
}

function generateConstantNonzero() {
  const deg = randInt(3, 5);
  const coeffs = Array.from({ length: deg + 1 }, () => nonzeroRand(-20, 20));
  const p0 = coeffs[0];
  setMath("constant-nonzero-problem", `\\left(${operatorTex(coeffs)}\\right)y=1`);
  setMath("constant-nonzero-answer", answerTex([
    `y=${fracToTex(new Fr(1, p0))}`
  ]));
}

function generateConstantZero() {
  const r = randInt(2, 4);
  const minQDeg = Math.max(1, 3 - r);
  const maxQDeg = Math.min(3, 6 - r);
  const qdeg = randInt(minQDeg, maxQDeg);
  const q = randomOperatorCoeffs(qdeg, true);
  const q0 = q[0];
  const ypCoeff = new Fr(1, fact(r) * q0);
  const yp = new Array(r + 1).fill(null).map(() => new Fr(0));
  yp[r] = ypCoeff;
  setMath("constant-zero-problem", `\\left(${operatorTex(q)}\\right)${dPowerTex(r)}y=1`);
  setMath("constant-zero-answer", answerTex([
    `y=${polynomialFromFractionsTex(yp)}`
  ]));
}

function generateMonomialNonzero() {
  const m = randInt(1, 4);
  const deg = randInt(1, 3);
  const p = randomOperatorCoeffs(deg, true);
  const rhs = new Array(m + 1).fill(0).map((_, i) => i === m ? 1 : 0);
  const yp = applyInverseOperator(p, rhs);
  setMath("monomial-nonzero-problem", `\\left(${operatorTex(p)}\\right)y=${monomialTex(m)}`);
  setMath("monomial-nonzero-answer", answerTex([
    `y=${polynomialFromFractionsTex(yp)}`
  ]));
}

function generateMonomialZero() {
  const r = randInt(1, 3);
  const mChoices = range(1, 3).filter(m => r + m <= 4);
  const m = mChoices[randInt(0, mChoices.length - 1)];
  const qdeg = randInt(1, 2);
  const q = randomOperatorCoeffs(qdeg, true);
  const rhs = new Array(m + 1).fill(0).map((_, i) => i === m ? 1 : 0);
  const u = applyInverseOperator(q, rhs);
  const yp = integratePoly(u, r);
  setMath("monomial-zero-problem", `\\left(${operatorTex(q)}\\right)${dPowerTex(r)}y=${monomialTex(m)}`);
  setMath("monomial-zero-answer", answerTex([
    `y=${polynomialFromFractionsTex(yp)}`
  ]));
}

function generatePolynomialNonzero() {
  const pdeg = randInt(1, 3);
  const rhs = randomPolynomialCoeffs(pdeg);
  const deg = randInt(1, 3);
  const p = randomOperatorCoeffs(deg, true);
  const yp = applyInverseOperator(p, rhs);
  setMath("polynomial-nonzero-problem", `\\left(${operatorTex(p)}\\right)y=${polynomialTex(rhs)}`);
  setMath("polynomial-nonzero-answer", answerTex([
    `y=${polynomialFromFractionsTex(yp)}`
  ]));
}

function generatePolynomialZero() {
  const r = randInt(1, 2);
  const pdeg = randInt(1, 2);
  const rhs = randomPolynomialCoeffs(pdeg);
  const qdeg = randInt(1, 2);
  const q = randomOperatorCoeffs(qdeg, true);
  const u = applyInverseOperator(q, rhs);
  const yp = integratePoly(u, r);
  setMath("polynomial-zero-problem", `\\left(${operatorTex(q)}\\right)${dPowerTex(r)}y=${polynomialTex(rhs)}`);
  setMath("polynomial-zero-answer", answerTex([
    `y=${polynomialFromFractionsTex(yp)}`
  ]));
}

window.addEventListener("load", function() {
  generateConstantNonzero();
  generateConstantZero();
  generateMonomialNonzero();
  generateMonomialZero();
  generatePolynomialNonzero();
  generatePolynomialZero();
});
</script>
